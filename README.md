# B2B Billing API - Lab Baseline

FastAPI billing service used in the AI-Augmented SDLC Transformation training.
Students: fork this repository. Do not clone it directly.

## Local run (optional smoke test)
pip install -r requirements.txt
uvicorn main:app --reload